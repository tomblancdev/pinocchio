# Docker Agent MCP - Environment Configuration
# Copy this file to .env and customize as needed
# Most values are auto-detected by run-mcp.sh, so this file is optional

# Projects root - the directory that will be mounted for workspace verification
# Agents can only access paths under this directory
# Default: $HOME (your home directory)
# PROJECTS_ROOT=/home/youruser/projects

# Claude credentials directory
# Default: ~/.claude
# HOST_CLAUDE_DIR=/home/youruser/.claude

# Config directory for Pinocchio (stores workspace allowlist)
# Default: ~/.config/pinocchio
# HOST_CONFIG_DIR=/home/youruser/.config/pinocchio

# Host user/group IDs (for file permissions in containers)
# Default: auto-detected from current user
# HOST_UID=1000
# HOST_GID=1000

# Maximum agent timeout in milliseconds (24 hours default)
# ABSOLUTE_MAX_TIMEOUT=86400000
